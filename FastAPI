#코루틴
다양한 진입점과 다양한 탈출점이 있는 루틴이다.
파이썬 비동기 함수는 코루틴 함수로 만들 수 있다.

async/await 문법으로 선언된 코루틴
코루틴 함수를 가지고 동시성을 지켰다.



## ASGI
# WSGI(web service gateway interface) 를 수정한 것이 ASGI 다.
# ASGI 는 비동기 적인것을 수행하기 위해 고안된 것

# 파이썬으로 만든 프로그램과 서버 사이에서 연결 하는 입구 역할이 ASGI 다.
# ASGI는 애플리케이션 프로그램(FastAPI)의 실행 결과를 웹 서버에 전달해주며, 웹 서버는 ASGI로부터 전달받은 응답 결과를 웹 클라이언트(브라우저)에 전송한다.


# Uvicon
# Uvicon은 ASGI 서버의 구현체
# uvloop, httptools를 사용하는 ASGI 웹 서버
